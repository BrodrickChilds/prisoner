.game-internal

  =image_tag(picture)
  = opponent
  %br
  .respond
    =link_to"A", game_respond_path(game, :strategy => false), :method => :put, :class => "game_respond light-link btn btn-success"
    =link_to "B", game_respond_path(game, :strategy => true), :method => :put, :class => "game_respond light-link btn btn-danger"
