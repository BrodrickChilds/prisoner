.game-internal

  =image_tag(picture)
  = opponent
  .respond
    =link_to "Betray", game_respond_path(game, :strategy => true), :method => :put, :class => "game_respond light-link"
    =link_to "Don't Betray", game_respond_path(game, :strategy => false), :method => :put, :class => "game_respond light-link"


